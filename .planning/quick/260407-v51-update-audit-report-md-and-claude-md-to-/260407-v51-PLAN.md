---
phase: quick
plan: 260407-v51
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/quick/260407-qhd-resystausa-com/AUDIT-REPORT.md
  - CLAUDE.md
autonomous: true
requirements: []

must_haves:
  truths:
    - "AUDIT-REPORT.md Section 5 header states forms are now hidden via CSS, not broken"
    - "All 5 CF7 form rows in the Active CF7 Forms table show status HIDDEN (CSS), not BROKEN"
    - "AUDIT-REPORT.md notes the hide-forms.py script location"
    - "CLAUDE.md Known-fixed list includes CF7 forms and Flodesk newsletter entry"
    - "CLAUDE.md Conventions section documents the .wpcf7 hide pattern and restore steps"
  artifacts:
    - path: ".planning/quick/260407-qhd-resystausa-com/AUDIT-REPORT.md"
      provides: "Updated form inventory with HIDDEN (CSS) status and script reference"
    - path: "CLAUDE.md"
      provides: "Updated Known-fixed list and new Conventions entry for CSS form hiding"
  key_links:
    - from: "AUDIT-REPORT.md Section 5"
      to: "hide-forms.py"
      via: "Script location note"
      pattern: "hide-forms.py"
---

<objective>
Update AUDIT-REPORT.md and CLAUDE.md to reflect that CF7 forms and the Flodesk newsletter widget are now hidden via CSS injection across all 607 static HTML pages (task 260407-uol completed).

Purpose: Keep documentation accurate so future work does not re-attempt to fix forms that are already handled, and so the CSS injection pattern is documented for restoration if needed.
Output: Two updated files — AUDIT-REPORT.md with HIDDEN (CSS) status and a known-fixed note; CLAUDE.md with a new Known-fixed entry and a Conventions entry documenting the pattern.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/quick/260407-qhd-resystausa-com/AUDIT-REPORT.md
@CLAUDE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update AUDIT-REPORT.md Section 5 — mark CF7 forms as HIDDEN (CSS)</name>
  <files>.planning/quick/260407-qhd-resystausa-com/AUDIT-REPORT.md</files>
  <action>
Make the following edits to AUDIT-REPORT.md:

1. In the Executive Summary "Known-fixed (skip):" line, append a third entry:
   "CF7 forms and Flodesk newsletter hidden via CSS injection (`hide-forms.py`)."
   The line currently reads:
   "**Known-fixed (skip):** reCAPTCHA removed from all pages. Super Store Finder replaced with static distributor cards."
   Change to:
   "**Known-fixed (skip):** reCAPTCHA removed from all pages. Super Store Finder replaced with static distributor cards. CF7 forms and Flodesk newsletter hidden via CSS injection (`hide-forms.py`)."

2. At the top of Section 5 (Form Inventory), immediately after the `## 5. Form Inventory` heading and before the `### Active CF7 Forms` subheading, insert this block:

   > **Known-fixed (2026-04-07):** All CF7 forms and the Flodesk newsletter widget (`.resnewsletter`) are now hidden via CSS injection across 607 HTML pages. The injector script is at `resysta-backup/hide-forms.py` and is idempotent. The injected CSS block is identified by the comment `<!-- gsd:hide-forms -->`. Status column below updated to reflect this.

3. In the Active CF7 Forms table, change the Status column for all 5 rows from "BROKEN" to "HIDDEN (CSS)":
   - General inquiry (ID: 19875): BROKEN → HIDDEN (CSS)
   - Homepage CTA (ID: 22816): BROKEN → HIDDEN (CSS)
   - Contact us (ID: 1004): BROKEN → HIDDEN (CSS)
   - Warranty registration (ID: 1005): BROKEN → HIDDEN (CSS)
   - Course registration (ID: 30013): BROKEN → HIDDEN (CSS)

4. In Section 9 Priority Action List, under "HIGH — Fixes needed for staging site to be useful", update entries H1 through H4. Each currently says the form is broken. Prepend each row's Action cell with "(Forms hidden — restore then replace if needed)" — or alternatively add a note row at the top of the HIGH table:
   > **Note:** CF7 forms H1–H4 are currently hidden via CSS (`hide-forms.py`). To replace with a static form service, first remove the `.wpcf7 { display: none }` CSS block, then implement the Formspree/Netlify replacement.
   Insert this as a paragraph before the HIGH table, not as a table row.

Do NOT change any other content in the file.
  </action>
  <verify>
Search the file for "HIDDEN (CSS)" — should appear exactly 5 times (one per form row).
Search for "hide-forms.py" — should appear at least 2 times (Executive Summary and Section 5 note).
Search for "gsd:hide-forms" — should appear once (in the Section 5 note).
  </verify>
  <done>
All 5 CF7 form rows show "HIDDEN (CSS)" status. Section 5 has a known-fixed callout block. Executive Summary Known-fixed line includes the CSS injection entry. Section 9 HIGH table has a restoration note before it.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md — Known-fixed entry and Conventions documentation</name>
  <files>CLAUDE.md</files>
  <action>
Make the following edits to CLAUDE.md:

1. Locate the "Known-fixed (skip):" line in the Executive Summary block (inside the GSD project-start block near the top). It currently reads:
   "**Known-fixed (skip):** reCAPTCHA removed from all pages. Super Store Finder replaced with static distributor cards."
   Change to:
   "**Known-fixed (skip):** reCAPTCHA removed from all pages. Super Store Finder replaced with static distributor cards. CF7 forms and Flodesk newsletter hidden via CSS injection (`hide-forms.py`)."

   NOTE: The Known-fixed line is inside the `<!-- GSD:project-start ... -->` block in CLAUDE.md. Preserve all surrounding GSD comment tags exactly — do not remove or alter them.

2. Locate the Conventions section. It currently reads:
   ```
   ## Conventions

   Conventions not yet established. Will populate as patterns emerge during development.
   ```
   Replace the body with the following (keep the `## Conventions` heading and the GSD comment tags intact):

   ```
   ## Conventions

   ### CSS Form Hiding Pattern

   **Pattern:** Inject a `<style>` block before `</head>` to visually suppress non-functional WordPress elements in the static backup.

   **Injected block (identified by comment `<!-- gsd:hide-forms -->`):**
   ```html
   <!-- gsd:hide-forms -->
   <style>
   .wpcf7 { display: none !important; }
   .resnewsletter { display: none !important; }
   </style>
   ```

   **Script:** `resysta-backup/hide-forms.py` — idempotent, safe to re-run.
   **Scope:** Applied to 607 HTML files in `resysta-backup/site/`.
   **Hidden elements:**
   - `.wpcf7` — all Contact Form 7 form containers (5 form IDs: 19875, 22816, 1004, 1005, 30013)
   - `.resnewsletter` — Flodesk newsletter widget on `index.html`

   **To restore (show forms again):** Remove the `<!-- gsd:hide-forms -->` comment and the `<style>` block that follows it from each affected HTML file. The script does not provide an undo — use git to revert, or write a reverse script targeting the `<!-- gsd:hide-forms -->` marker.

   **Why hidden instead of removed:** CSS injection is fast and reversible. Full DOM removal would require parsing each file and risks breaking surrounding layout. When a static form replacement (Formspree, Netlify Forms) is implemented, remove the CSS block and wire the replacement form in the same pass.
   ```

   Preserve the `<!-- GSD:conventions-start ... -->` and `<!-- GSD:conventions-end -->` comment tags that wrap this section — do not remove them.
  </action>
  <verify>
Search CLAUDE.md for "hide-forms.py" — should appear at least 2 times (Known-fixed line + Conventions section).
Search for "gsd:hide-forms" — should appear once (in the Conventions code block).
Search for "GSD:conventions-start" — should still be present (tag not removed).
Search for ".wpcf7" — should appear in the Conventions section.
  </verify>
  <done>
CLAUDE.md Known-fixed line includes the CSS injection entry. Conventions section is populated with the hide pattern, script reference, scope, hidden selectors, and restore instructions. GSD comment tags are intact.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local filesystem only | All edits are to local documentation files — no external services, no user input |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-v51-01 | Tampering | GSD comment tags in CLAUDE.md | mitigate | Explicitly instruct executor to preserve `<!-- GSD:* -->` wrapper tags — verify tags still present after edit |
| T-v51-02 | Tampering | AUDIT-REPORT.md table formatting | accept | Markdown tables are plain text — minor formatting drift is acceptable for a documentation file |
</threat_model>

<verification>
After both tasks complete:

1. `grep -c "HIDDEN (CSS)" "G:/01_OPUS/Projects/resystausa/.planning/quick/260407-qhd-resystausa-com/AUDIT-REPORT.md"` → must return `5`
2. `grep -c "hide-forms.py" "G:/01_OPUS/Projects/resystausa/.planning/quick/260407-qhd-resystausa-com/AUDIT-REPORT.md"` → must return `2` or more
3. `grep -c "hide-forms.py" "G:/01_OPUS/Projects/resystausa/CLAUDE.md"` → must return `2` or more
4. `grep "GSD:conventions-start" "G:/01_OPUS/Projects/resystausa/CLAUDE.md"` → must match (tag preserved)
5. `grep "GSD:project-start" "G:/01_OPUS/Projects/resystausa/CLAUDE.md"` → must match (tag preserved)
</verification>

<success_criteria>
- AUDIT-REPORT.md Section 5: all 5 CF7 form rows show "HIDDEN (CSS)" status
- AUDIT-REPORT.md Section 5: known-fixed callout block present with script reference
- AUDIT-REPORT.md Executive Summary: Known-fixed line includes CSS injection entry
- AUDIT-REPORT.md Section 9 HIGH: restoration note present before H1-H4 table
- CLAUDE.md Known-fixed line: includes CSS injection entry
- CLAUDE.md Conventions: populated with hide pattern, selectors, script path, restore instructions
- CLAUDE.md GSD comment tags: all intact, no structural damage
</success_criteria>

<output>
After completion, create `.planning/quick/260407-v51-update-audit-report-md-and-claude-md-to-/260407-v51-SUMMARY.md`
</output>
